{
  "kind": "collectionType",
  "collectionName": "redirects",
  "info": {
    "name": "Redirects"
  },
  "options": {
    "increments": true,
    "timestamps": true,
    "draftAndPublish": false
  },
  "attributes": {
    "from": {
      "type": "string",
      "required": true
    },
    "to": {
      "type": "string",
      "required": true
    },
    "type": {
      "type": "enumeration",
      "enum": [
        "permanently",
        "temporarily"
      ],
      "default": "permanently",
      "required": true
    }
  }
}
